const para = document.querySelectorAll(".toggle");
console.log(para);

/* for (let i = 0; i < para.length; i++) {
  console.log(para[i]);
} */

para.forEach((ele) => console.log(ele));

// this is just a testing file for the nodelist.
